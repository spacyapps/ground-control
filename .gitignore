# Xcode / SwiftPM
.build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
Package.resolved

# Build artifacts
build/
*.app
*.dmg
*.zip

# macOS
.DS_Store

# Secrets / signing — NEVER commit
*.p12
*.cer
*.p8
*.mobileprovision
*.provisionprofile
Secrets.xcconfig
.env
*.key

# Editor
.idea/
*.swp

# Sellable artwork — lives on disk, ships as its own zip, never in git.
# Removed from history 2026-08-16 before the repo was first published.
ExtraThemes/
