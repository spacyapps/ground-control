# Xcode / SwiftPM
.build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
Package.resolved

# Build artifacts
build/
*.app
*.dmg
*.zip

# macOS
.DS_Store

# Secrets / signing — NEVER commit
*.p12
*.cer
*.p8
*.mobileprovision
*.provisionprofile
Secrets.xcconfig
.env
*.key

# Editor
.idea/
*.swp

# Subagent local memory — per-project, deliberately not versioned (memory: local)
.claude/agent-memory-local/

# Sellable artwork — lives on disk, ships as its own zip, never in git.
# Removed from history 2026-08-16 before the repo was first published.
ExtraThemes/

# Working notes that were never written for a reader — kept off the repo on
# purpose 2026-09-17, and listed here so they cannot walk back in.
docs/PUBLISHING.md
docs/THEME-DELIVERY.md
